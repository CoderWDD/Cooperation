#debug: true

server:
  port: 3345

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cooperation?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 123456qq.
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations:
    - classpath*:/mapper/**/*.xml
